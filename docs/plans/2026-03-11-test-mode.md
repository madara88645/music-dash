# Test Mode Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Oyuna `?testMode=1` ile acilan ayri bir test modu eklemek ve health sistemini davranis seviyesinde dogrulamak.

**Architecture:** Saf helper fonksiyonlar ayri bir browser-compatible dosyada toplanacak. `public/game.js` bu helper'lari kullanarak aktif guard setini ve temas hasarini belirleyecek. Normal mod etkilenmeyecek, test modu ise yakin guard ile hizli playtest verecek.

**Tech Stack:** Plain JavaScript, HTML5 Canvas, Node.js test runner

---

### Task 1: Helper davranis testlerini yaz

**Files:**
- Create: `test/game-helpers.test.js`

**Step 1: Write the failing test**

Su davranislari test et:
- `resolveGameMode('?testMode=1')` test modunu acar
- `buildGuardLoadout({ testMode: false })` bos liste dondurur
- `buildGuardLoadout({ testMode: true })` en az bir guard dondurur
- `applyContactDamage()` esit toplam surede ayni sonucu verir
- `formatHealthValue(99.55)` sagligi yukari yuvarlamaz

**Step 2: Run test to verify it fails**

Run: `npm test`
Expected: FAIL because helper module does not exist yet.

### Task 2: Minimal helper modulu ve entegrasyon

**Files:**
- Create: `public/game-helpers.js`
- Modify: `public/index.html`
- Modify: `public/game.js`

**Step 1: Write minimal implementation**

- Browser ve Node icin ortak helper dosyasi yaz
- `public/index.html` icinde helper script'ini `game.js` oncesi ekle
- `public/game.js` icinde query param'dan mod sec
- Guard dizisini helper uzerinden kur
- Temas hasarini saniye bazli hesapla
- Health metnini daha dogru integer gosterecek sekilde guncelle

**Step 2: Run tests to verify they pass**

Run: `npm test`
Expected: PASS

### Task 3: Final verification

**Files:**
- Modify: `README.md` if needed

**Step 1: Add a short README note**

Test modu URL'ini README'ye kisaca ekle.

**Step 2: Run syntax verification**

Run: `node --check public/game.js`
Expected: PASS

**Step 3: Run full tests again**

Run: `npm test`
Expected: PASS
